# CLAUDE.md — daily-order-report (주식회사 디엘나인)

> **매출봇** — 카페24 + 네이버 스마트스토어 주문을 GitHub Actions cron(매일 3회)으로 수집해
> 카카오톡 리포트 발송 + `daily_sales` 집계를 bks-os(웹디비/매출보드)로 전송하는 Python 봇.
> ⚠️ 매출 트랙 이원화: 이 레포는 **COMMANINE(온라인 커머스) 전용** — 경량랙 B2B(new-biz-intake-os)와 무관.

## 시작 전 필독

- 회사 상태·재사용 표: bks-os `docs/jarvis/회사-현재상태.md` (SoT). 웹디비 커넥터 있으면 `get_company_state`.
- **웹디비 규약**: 저장 실패 시 반드시 "웹디비 저장 실패" 실토. 된 척 금지.

## AI 작업 표준 (정본: bks-os docs/jarvis/17 — 변경은 BK 승인 커밋으로만)

- 발동: 새 모듈·화면·시스템 또는 스키마·업무흐름을 바꾸는 개조 = 큰 작업.
- 큰 작업 전: ①교본 자가문답(PMBOK·SCOR·ERP, 분야별 100~200문, 표준↔현재 O/△/X — BK에게 안 떠넘김)
  ②현장엔 ★delta만 질문 ③RTM(질문→답→평가→승인→수용기준→구현→눈검증→완료) ④사이클 병행 금지.
- **눈검증 철칙 (이 레포 특화)**: 화면이 없는 봇이므로 완료 판정은
  **실행 로그(Actions run)·실제 발송된 카톡 리포트·`daily_sales` DB 조회 결과**를 사람이 확인한 기록으로만.
  코드·테스트·mock 통과는 완료가 아니다. 버그 수리도 눈검증은 필수.
- 긴급 장애(리포트 미발송 등)는 즉시 수리하되 눈검증 생략 불가, 문답·RTM은 24h 내 사후 기록.
- "ㄱㄱ"·"빨리" = 착수 승인이지 게이트 생략 아님. 생략은 "게이트 생략"이라 명시된 때만.
- 자산 확인 4단계(작업 시작 시, 체크리스트로 표시): ①get_company_state(또는 SoT)
  ②search_vault("자산맵 <도메인>") ③search_vault(선례) ④로컬 자산 `C:\Users\naked\Documents\02.Void.O\_index`.
  건너뛴 단계는 ⚠️미실행+이유 표기.
- 새 레포·새 프로젝트 첫 커밋에 이 표준 블록 복사(상속). 서브에이전트·타세션 지시문에도 포함(위임).
- 답 없이 지은 것은 가설 — 가설에 기능을 얹지 않는다. 기록은 docs/*.md 커밋(웹디비 수집).
  확인된 것만 "됐다"고 말한다 — 실패는 "실패"라고 실토한다.

## 개발 규칙

- API 토큰·시크릿(카페24·네이버 커머스·카카오)은 GitHub Actions 시크릿에만 — 절대 코드·로그·git 노출 금지.
- 주문·매출 수치는 내부 전용 — 외부 서비스·공개 레포에 내보내지 않는다.
- cron·API 클라이언트 수정 시 mock 테스트(`tests/`) 통과 후, **실제 Actions 실행 로그와 수신 리포트 확인**까지가 완료.
